# Done

Completed TODO items — moved here by the implement agent (on the impl branch) or by
hand, with date and ref. Keeps `TODO.md` lean; this file + `AGENT_LOG.md` are the
traceable history. Also the idempotency anchor: items listed here are skipped.

<!-- Format per entry:
- YYYY-MM-DD — <item title> (autoplan/impl @ <commit>) — short note, tests [color].
-->
